{
  "name": "@mocchi/docker",
  "version": "0.0.0",
  "description": "Docker and Docker Compose integration for Mocchi.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
