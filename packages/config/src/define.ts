import type { MocchiConfig } from './types.js'

/**
 * Provides type inference and validation hints for a Mocchi config file.
 */
export function defineMocchiConfig(config: MocchiConfig): MocchiConfig {
  return config
}
