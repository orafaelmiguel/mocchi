{
  "name": "@mocchi/config",
  "version": "0.0.0",
  "description": "Configuration loading and validation for Mocchi.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "tsup": "^8.5.1"
  }
}
